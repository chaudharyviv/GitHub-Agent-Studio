"""
Orchestration module for agent coordination.

Provides the runner functions that execute investigations in either
single-agent or multi-agent modes, managing the overall flow and
sharing state between agents via the memory layer.
"""
